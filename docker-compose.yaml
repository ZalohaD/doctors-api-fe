services:
  react-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: react-frontend
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    networks:
      - frontend
    stdin_open: true
    tty: true

networks:
  frontend:
    driver: bridge